---
title: "Don't waste SAM"
title_zh: 不要浪费SAM
authors: "Nermeen Abou Baker, Uwe Handmann"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10696v1"
tags: ["query:raw-seg"]
score: 8.0
evidence: 微调SAM用于废物分割，图像分割技术
tldr: SAM在零样本分割上表现优异，但在垃圾分割等特定领域精度不足。本文使用三个真实场景垃圾数据集对SAM进行微调，发现微调后的SAM-ViT-H模型在Zerowaste和TACO上IoU提升高达+30，接近全监督方法TrashCan 1.0。结果表明，微调SAM作为基础模型可有效泛化至下游任务，避免资源浪费。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10696v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1185, \"height\": 246, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10696v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1283, \"height\": 896, \"label\": \"Table\"}]"
motivation: SAM在通用分割中表现优异，但在垃圾分割等复杂场景下精度有限，亟需评估其泛化能力并探索微调策略。
method: 使用Zerowaste、TACO和TrashCan 1.0三个真实垃圾数据集，对SAM-ViT-H模型进行全参数微调。
result: 微调后SAM在Zerowaste和TACO上IoU分别提升30+，与TrashCan 1.0的差距缩小至仅-1.44。
conclusion: 微调SAM作为基础模型能显著提升下游垃圾分割任务的性能，不应忽视其潜力。
---

## 摘要
Meta AI最近发布了Segment Anything Model（SAM），该模型在各种任务中展示了卓越的零样本图像分割性能，具有惊人的准确性。尽管SAM无法在多个研究领域提供准确的分割，但它仍然是支持分割流程的有价值起点，特别是对于那些需要大量高级技能标注的任务。本研究旨在评估SAM的泛化能力，并使用三个废物分割数据集对SAM模型进行微调。尽管这些数据集是从真实场景中捕获的，与SAM预训练时类似，但它们存在一些挑战，包括遮挡、可变形物体、透明物体以及与背景易混淆的物体。在我们的发现中，微调的SAM-ViT-H模型在Zerowaste和TACO数据集上的IoU显著提高了+30，超越了现有最佳方法，并且与TrashCan 1.0的性能水平非常接近，仅差-1.44。在评估了这些流行的废物数据集后，很明显，将SAM作为基础模型进行微调是为下游废物分割任务提供更好泛化能力的关键步骤。因此，不应忽视或浪费SAM。

## Abstract
Meta AI has recently released the Segment Anything Model (SAM), which demonstrates exceptional zero-shot image segmentation performance across various tasks with remarkable accuracy. Despite its inability to provide accurate segmentation across multiple research fields, SAM still serves as a valuable starting point for supporting the segmentation pipeline process, particularly for tasks that require extensive and senior skills annotations. This study aims to evaluate the generalization of SAM and fine-tuning SAM models using three waste segmentation datasets. Although they are captured from real scenes as SAM was pretrained on, these datasets present several challenges, including occlusions, deformable objects, transparency, and objects easily confused with backgrounds. In our findings, the fine-tuned SAM-ViT-H model outperforms the state-ofthe-art Zerowaste, and TACO datasets with a significant increase of +30 in IoU, and it closely approaches performance levels of TrashCan 1.0, with only a -1.44 difference. After evaluating these popular waste datasets, it became evident that fine-tuning SAM as a foundational model is a crucial step for providing better generalization for downstream waste segmentation tasks. Therefore, SAM should not be disregarded or wasted.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：Meta AI发布的Segment Anything Model（SAM）在零样本图像分割上表现优异，但在特定领域（如废物分割）中，由于物体遮挡、可变形、透明、与背景易混淆等挑战，SAM直接应用（零样本）效果有限。本研究旨在评估SAM在真实废物场景下的泛化能力，并探索通过微调是否能够显著提升下游任务的性能。
- **整体含义**：作者认为，尽管SAM在某些任务上表现欠佳，但不应“浪费”其作为基础模型的潜力。通过微调，SAM能够接近甚至超越现有专门训练的分割模型，因此应当被充分利用而非忽视。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：使用预训练的SAM作为基础模型，仅微调其掩码解码器（mask decoder），保持图像编码器（ViT）和提示编码器（prompt encoder）冻结。微调过程中使用边界框（bbox）提示（来源于真实标注的边界框）作为输入。
- **关键技术细节**：
  - 损失函数：`20 × focal loss + dice loss + MSE loss`（组合损失）。
  - 优化器：Adam，学习率 `8e-4`，批次大小 `batch-size=8`。
  - 早停机制（early stopping）防止过拟合。
  - 使用三个版本的SAM：ViT-B（91M参数）、ViT-L（308M参数）、ViT-H（636M参数）。
- **流程文字说明**：加载预训练SAM权重 → 冻结图像编码器和提示编码器 → 初始化掩码解码器 → 在训练循环中仅更新解码器参数 → 使用边界框提示和真实掩码计算组合损失 → 反向传播优化。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**（三个废物分割数据集）：
  - **Zerowaste**：4,661帧，全多边形标注，包含可变形物体（纸板、软塑、硬塑、金属），环境杂乱（传送带场景）。
  - **TrashCan 1.0**：7,212张水下垃圾、植物、动物图像，用于海洋机器人自主垃圾分割。
  - **TACO**：1,500张野外垃圾图像（森林、道路、海滩），标注粗糙，背景、光照、物体大小变化极大。
- **基准（baseline）**：DeepLabv3+（Zerowaste和TACO的SOTA）、原始SAM的零样本结果（SAM-ViT-B/L/H）、RITM（交互式分割模型，仅TrashCan）。
- **对比方法**：
  - 原始SAM（零样本）三个版本。
  - 微调SAM三个版本（ViT-B/L/H）。
  - DeepLabv3+（仅作为参考，非本文实现）。

## 4. 资源与算力

- 文中明确说明：单张NVIDIA Quadro RTX 8000 GPU，显存48GB。
- 训练轮数：Zerowaste和TrashCan各5个epoch，TACO 10个epoch（因为TACO数据更少且更困难）。
- 未说明训练总时间或能耗，但使用了早停。

## 5. 实验数量与充分性

- **实验数量**：共3个数据集 × 3种SAM骨干（原始+微调）= 9组主要实验。另列出DeepLabv3+的已有结果作为参考。无额外消融实验（如不同损失函数、是否冻结编码器、不同prompt模式等）。
- **充分性评估**：
  - **优点**：覆盖了三个不同难度和来源的废物数据集，具有代表性；对比了多个基线（DeepLabv3+、SAM零样本、RITM）。
  - **不足**：未进行消融实验（如仅微调解码器 vs 全参数微调、不同损失权重、不同prompt等）；仅使用边界框提示（未验证点提示或文本提示）；未在相同条件下复现DeepLabv3+结果（直接引用原论文数据），可能存在评估协议不一致。总体而言，实验设计偏向验证可行性，而非全面分析。

## 6. 论文的主要结论与发现

- 微调SAM-ViT-H在Zerowaste上达到82.79 mIoU，相比DeepLabv3+（52.13）提升+30.66；相比原始SAM零样本（61.12）提升+21.67。
- 在TrashCan 1.0上，微调SAM-ViT-H达到78.88 mIoU，仅比RITM（80.32，原文提及）低-1.44；但相比原始SAM零样本（66.43）提升+12.45。
- 在TACO上，微调SAM-ViT-H达到61.06 mIoU，远高于DeepLabv3+（29.19）和原始SAM（46.29），提升显著。
- 结论：微调SAM作为基础模型能显著提升下游废物分割任务的泛化能力，不应被“浪费”。

## 7. 优点

- **方法简洁有效**：仅微调解码器，计算开销小，且性能提升巨大，证明了基础模型微调的潜力。
- **问题选择有意义**：废物分割具有现实应用价值（自动垃圾分类），且数据集具有多种挑战（变形、遮挡、透明等），能充分展示SAM的局限性及微调后的改善。
- **对比全面**：涵盖了从轻量到重量级的SAM变体，并报告了精度、召回、F1、像素准确率等多维度指标。
- **结果可视化**：提供了分割结果对比图（图1），直观说明微调对细节的改善。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅使用边界框prompt，未尝试点提示或文本提示，也未探讨无提示的全自动模式（everything mode）。
  - 未做消融实验，例如：是否全参数微调、不同损失权重、不同学习率等超参数敏感性。
  - 仅在废物分割领域测试，未验证其他复杂场景（如医学、遥感）。
- **评估偏差风险**：
  - 引用的DeepLabv3+结果来自其他论文，可能使用不同训练配置、数据划分或预处理，导致比较不公平。
  - 实验仅报告均值指标，未给出标准差或置信区间，难以判断统计显著性。
  - 数据集规模较小（尤其是TACO仅1500张），微调结果可能过拟合特定分布，泛化性需进一步验证。
- **应用限制**：
  - 微调需要标注数据（边界框），对于无标注场景仍需依赖零样本先进行伪标注。
  - ViT-H版本（636M参数）计算资源需求高，实际部署可能受限。
  - 文中未讨论实时性、推理速度等实用性问题。

（完）
