---
title: Weakly Supervised Incremental Segmentation via Semantic Anchors and Spatial Arbitration
title_zh: 通过语义锚点与空间仲裁的弱监督增量分割
authors: "Zhonggai Wang, Kai Fang, Guangyu Gao"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.04060v1"
tags: ["query:raw-seg"]
score: 9.0
evidence: 弱监督增量语义分割，对应语义分割任务
tldr: 弱监督增量语义分割面临连续噪声监督导致特征漂移和语义覆盖问题。本文提出SASA方法，通过语义锚点（可学习令牌）保持长期语义身份，配合弹性残差适应进行实例级精细调整，并设计空间标签仲裁机制过滤不可靠信号。实验表明SASA在多步增量设置下显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04060v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 890, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04060v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1789, \"height\": 1007, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04060v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 916, \"height\": 624, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04060v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 924, \"height\": 638, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04060v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 643, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04060v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 736, \"height\": 477, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04060v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1867, \"height\": 761, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04060v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 770, \"height\": 260, \"label\": \"Table\"}]"
motivation: 弱监督增量语义分割中噪声监督导致特征漂移，新类覆盖旧类。
method: 提出SASA，包括语义锚点（可学习令牌）维持语义稳定性，弹性残差适应实现实例级调整，空间标签仲裁过滤不可靠信号。
result: 在标准基准上多步增量设置下持续超越现有方法。
conclusion: SASA有效缓解弱监督下的特征漂移，提升增量分割性能。
---

## 摘要
弱监督增量学习语义分割（WILSS）面临持续引入噪声监督的问题，这会逐步破坏类别级表示，导致严重的特征漂移和语义退化，从而使新学到的类别覆盖旧类别。为解决这些问题，我们提出一种抗漂移的WILSS方法，命名为SASA，旨在通过语义锚点与空间仲裁稳定语义学习。具体而言，在表示层面，我们引入可学习令牌的语义锚点作为刚性类别级参考，以保持长期语义一致性；与此互补，弹性残差适应实现受控的实例特定微调，确保稳定且灵活的学习轨迹。在监督层面，我们开发了空间标签仲裁机制，该机制执行几何感知决策，直接过滤不可靠信号并强制执行严格的“一个对象，一个类别”约束。通过协同稳定表示并提高监督可靠性，SASA有效缓解了弱监督下的特征漂移。在标准基准上的大量实验表明，我们的方法持续优于现有最先进方法，尤其是在具有挑战性的多步增量设置中。代码可在 https://github.com/ZhonggaiWang/SASA 获取。

## Abstract
Weakly Incremental Learning for Semantic Segmentation (WILSS) suffers from the continuous introduction of noisy supervision, which progressively corrupts class-level representations, leading to severe feature drift and semantic corruption, thereby causing newly learned classes to overwrite old ones. To address these issues, we propose a drift-resilient WILSS approach, named SASA, designed to stabilize semantic learning via Semantic Anchors and Spatial Arbitration. Specifically, at the representation level, we introduce semantic anchors of learnable tokens as rigid class-level references to preserve long-term semantic identity. Complementary to this, an elastic residual adaptation facilitates controlled, instance-specific refinement, ensuring a stable yet flexible learning trajectory. At the supervision level, we develop a Spatial Label Arbitration mechanism that performs geometry-aware decisions to directly filter unreliable signals and enforce a strict "one object, one class" constraint. By synergistically stabilizing representations and improving supervision reliability, SASA effectively mitigates feature drift under weak supervision. Extensive experiments on standard benchmarks demonstrate that our approach consistently outperforms existing state-of-the-art methods, particularly in challenging multi-step incremental settings. The code is available at https://github.com/ZhonggaiWang/SASA.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：弱监督增量语义分割（WILSS）在实际场景中很有价值，因为新类别的密集标注往往不可获得，只能使用图像级标签。然而，现有方法通过融合CAM激活和旧模型预测生成伪标签，这种过程会持续引入噪声监督，导致新类特征向旧类簇漂移、旧类决策边界被侵蚀，最终出现**特征漂移（Feature Drift）** 和**类别覆盖（Class Overwriting）**。
- **整体含义**：本文旨在从根源上解决WILSS中噪声积累引发的特征漂移问题，提出一种“漂移鲁棒”的框架SASA，通过在表示层和监督层同时进行稳定化设计，实现长程增量学习下的语义保持。

## 2. 方法论

- **核心思想**：用**刚性语义锚点**作为稳定的类别参考，防止噪声直接扭曲类表示；用**弹性残差适应**吸收实例变化而不破坏语义结构；在监督层面引入**空间标签仲裁（SLA）**，利用几何先验过滤矛盾伪标签。
- **关键技术细节**：

  - **Drift-Resilient Semantic Anchors (DSA)**：
    - **刚性锚点**：为每个类别维护一个可学习令牌 \( \mathbf{A}_t \in \mathbb{R}^{|C_{0:t}|\times D} \)，作为该类在嵌入空间的稳定坐标。
    - **弹性残差令牌（ERT）**：通过交叉注意力机制（锚点作为查询，像素特征作为键/值）生成残差 \( \mathbf{R}_t \)，最终令牌 \( \mathbf{Z}_t = \mathbf{A}_t + \mathbf{R}_t \)，并施加 ℓ₂ 正则化（\( \mathcal{L}_{\text{reg}} = |\mathbf{R}_t|_2^2 \)）限制残差幅度。
    - **优化**：分割损失使用余弦相似度（公式2）；分离损失 \( \mathcal{L}_{\text{sep}} \) 要求最终令牌与自身锚点对齐、与其他锚点正交；蒸馏损失 \( \mathcal{L}_{\text{dist}} \) 约束旧类锚点不变。
  - **Spatial Label Arbitration (SLA)**：
    - 使用类无关分割器（如SAM）生成对象掩码 \( \{m_i\} \)。
    - 对每个掩码计算像素级可靠性权重（高斯衰减，重心处权重大）。
    - 根据新类信号密度 \( \rho_i \) 动态决定候选类别集 \( K \)：若 \( \rho_i > \tau \)（阈值0.6），则 \( K = C_t \)（新类）；否则 \( K = C_{0:t-1} \)。
    - 执行加权多数投票决定掩码标签，并传播至所有像素，实现“一个对象，一个类别”。
- **总目标**：\( \mathcal{L} = \mathcal{L}_{\text{cls}} + \lambda_{\text{seg}}\mathcal{L}_{\text{seg}} + \lambda_{\text{sep}}\mathcal{L}_{\text{sep}} + \lambda_{\text{dist}}\mathcal{L}_{\text{dist}} + \lambda_{\text{reg}}\mathcal{L}_{\text{reg}} \)。

## 3. 实验设计

- **数据集与场景**：
  - **Pascal VOC 2012**（增广至10,582张）：设置15-5、10-10（2步）、10-5（3步）、10-2（6步）。
  - **MS COCO**（80类）：用于跨数据集评估（COCO-to-VOC：先学60个COCO类，再增量学习剩余VOC类）。
  - 所有增量步骤均采用**重叠设置**（新旧类别共存）。
- **基准对比**：
  - **全监督**：MiB、PLOP、SDR、RECALL、ALIFE、DKD、STAR、BARM（像素级标签）。
  - **弱监督**：WILSON、FMWISS、Teddy、ToCo（图像级标签）。
  - 同时报告使用ResNet和ViT骨干的基线，以及全监督Joint（上界）。
- **评价指标**：初始类mIoU、新类mIoU、全集mIoU。

## 4. 资源与算力

- **原文未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。
- 仅提及采用**ViT-B**作为骨干，训练中使用了学习率预热和多项式衰减策略，增量步骤迭代次数为8k（VOC）或20k（COCO-to-VOC）。

## 5. 实验数量与充分性

- **实验组数**：
  - 表I：在4个VOC增量设置+1个COCO-to-VOC设置上比较了12种方法（全监督和弱监督）。
  - 表II：DSA和SLA的消融实验（4种组合）。
  - 表III：损失权重（\( \lambda_{\text{seg}}, \lambda_{\text{sep}}, \lambda_{\text{dist}} \)）的敏感性分析（共9组）。
  - 表IV：SLA阈值 \( \tau \) 的敏感性分析（5个值）。
  - 图3：t-SNE可视化特征空间。
  - 图4：定性分割结果对比。
- **充分性与公平性**：覆盖了从简单到挑战性（多步、跨数据集）的设置，对比的方法均为近年SOTA，消融实验逐一验证每个组件的贡献，敏感性分析展示了超参数影响。实验设计客观、全面。

## 6. 主要结论与发现

- SASA在所有设置下均**超越现有弱监督方法**，尤其在**多步增量**（10-2）和**跨数据集**（COCO-to-VOC）任务上提升显著（如10-2旧类mIoU比WILSON高22.4%，新类高16.1%）。
- DSA和SLA**互补**：DSA稳定表示（+4.7%），SLA净化监督（+3.5%），联合使用共提升7.6%。
- 损失权重 \( \lambda_{\text{seg}}=0.2, \lambda_{\text{sep}}=0.2, \lambda_{\text{dist}}=0.1 \) 为最优组合；SLA阈值 \( \tau=0.6 \) 平衡噪声抑制与监督充分性。
- 特征可视化表明SASA有效抑制特征漂移，保持新旧类别清晰边界。

## 7. 优点

- **方法创新**：首次在弱监督增量分割中引入**刚性语义锚点**作为长期类别参考，结合**弹性残差**实现稳定-灵活平衡，从根本上缓解漂移而非仅后处理标签。
- **空间仲裁机制**：利用类无关对象掩码进行几何感知的标签仲裁，直接解决同一对象内部新旧类别矛盾，概念简洁且有效。
- **实验全面**：覆盖多种难度设置，包含长步数、跨数据集最挑战场景，对比充分，消融和敏感性分析详实。
- **代码开源**：促进复现与后续研究。

## 8. 不足与局限

- **未报告训练资源**：缺少GPU型号、数量、训练时间等算力细节，影响可复现性评估。
- **依赖外部分割器**：SLA需要类无关分割器（如SAM），其质量对最终结果有潜在影响，未讨论分割器失效时的鲁棒性。
- **骨干限制**：仅测试ViT-B，未在CNN骨干或其他尺寸下验证泛化能力。
- **与全监督仍有差距**：尽管超越弱监督SOTA，但mIoU仍显著低于全监督Joint版（如VOC 10-5全监督78.2%，SASA 71.7%）。
- **阈值敏感**：阈值 \( \tau \) 需人工设定，不同数据集或增量步骤可能需调整，未提供自适应方案。

（完）
