---
title: "SegDINO: Introducing Multi-Scale Structure into DINO for Efficient Medical Image Segmentation"
title_zh: "SegDINO: 将多尺度结构引入DINO以实现高效医学图像分割"
authors: "Sicheng Yang, Hongqiu Wang, Zhaohu Xing, Sixiang Chen, Qiuxia Yang, Yize Mao, Guang Yang, Lei Zhu"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17972v1"
tags: ["query:raw-seg"]
score: 8.0
evidence: 基于DINOv3骨干的深度学习语义分割
tldr: 医学图像分割中，直接应用自监督DINO模型面临挑战，现有方法依赖复杂解码器带来大量参数。SegDINO引入多尺度结构，通过Token Pyramid Adaptation重构DINO特征为伪多尺度层级，结合Scale-Aware Decoding实现高效尺度内细化和自上而下传播。在PanCT等四个数据集上达到SOTA，且参数和计算开销更低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17972v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1242, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17972v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17972v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1147, \"height\": 513, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17972v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1240, \"height\": 492, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17972v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1030, \"height\": 312, \"label\": \"Table\"}]"
motivation: 现有DINO模型用于分割需复杂解码器，效率和性能不理想，而引入尺度信息比增加解码器容量更关键。
method: SegDINO以DINOv3为骨干，设计Token Pyramid Adaptation生成伪多尺度特征，再通过Scale-Aware Decoding进行尺度内细化与多尺度传播。
result: 在PanCT及三个公开数据集上，SegDINO取得最优分割性能，尤其在胰腺肿瘤小病灶上表现突出，且效率更高。
conclusion: 引入多尺度结构能有效提升DINO模型在医学图像分割中的效率与精度，为轻量级分割框架提供新思路。
---

## 摘要
自监督DINO模型提供了强大的可迁移视觉表示，但直接将它们应用于图像分割仍然具有挑战性。现有方法通常依赖带有复杂上采样的重型解码器，引入了大量参数和计算开销。我们观察到，向DINO特征引入尺度比增加解码器容量更为关键。在这项工作中，我们提出了SegDINO，一个高效的图像分割框架，它将DINOv3骨干网与轻量级尺度建模相结合。SegDINO引入了令牌金字塔适应（TPA）来将中间DINO特征重组为伪多尺度层次结构，以及尺度感知解码（SAD）用于高效的尺度内细化和自上而下的多尺度传播。我们进一步整理了PanCT，一个新的CT数据集，包含284名有专家标注的胰腺肿瘤患者，以评估SegDINO处理困难的微小病变病例的能力。在PanCT和三个公共基准上的大量实验表明，SegDINO以高效率实现了最先进的结果。代码可在https://github.com/script-Yang/segdino_v2获取。

## Abstract
Self-supervised DINO models provide strong transferable visual representations, yet applying them directly to image segmentation remains challenging. Existing approaches commonly rely on heavy decoders with complex upsampling, introducing substantial parameter and computational overhead. We observe that introducing scale into DINO features is far more critical than increasing decoder capacity. In this work, we present SegDINO, an efficient segmentation framework that integrates a DINOv3 backbone with lightweight scale modeling. SegDINO introduces Token Pyramid Adaptation (TPA) to reorganize intermediate DINO features into a pseudo multi-scale hierarchy, and Scale-Aware Decoding (SAD) for efficient intra-scale refinement and top-down multi-scale propagation. We further curate PanCT, a new CT dataset containing 284 patients with expert-annotated pancreatic tumors, to assess SegDINO's ability to handle difficult small-lesion cases. Extensive experiments on PanCT and three public benchmarks demonstrate that SegDINO achieves state-of-the-art results with high efficiency. The code is available at https://github.com/script-Yang/segdino_v2.

---

## 论文详细总结（自动生成）

# 论文《SegDINO: Introducing Multi-Scale Structure into DINO for Efficient Medical Image Segmentation》详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：自监督预训练的 DINO 系列模型（如 DINOv3）虽然能学到强大的可迁移视觉表示，但直接用于医学图像分割时效果不佳。现有方案通常依赖复杂的重型解码器（如多层上采样、密集融合），导致参数量和计算开销显著增加。
- **关键洞察**：作者指出，**向 DINO 特征中引入尺度信息（多尺度结构）比单纯增加解码器容量更为关键**。因此，目标是以轻量级方式为 DINO 特征添加多尺度层次，从而高效实现高精度医学图像分割。

## 2. 论文提出的方法论
### 2.1 整体框架
SegDINO 包含三个主要模块：
1. **编码器骨干**：使用预训练 DINOv3-S（冻结），从第 3、6、9、12 层提取中间 token 序列 \(Z_p^{(\ell_k)}\)。
2. **Token Pyramid Adaptation (TPA)**：将各层 token 重塑为 2D 特征图 \(F_k\)，经 1×1 卷积投影到统一通道空间 \(\tilde{F}_k\)，再通过轻量缩放算子 \(\rho_k\)（步长卷积）构建**伪多尺度金字塔**（尺度为 1/4、1/8、1/16、1/32）。
3. **Scale-Aware Decoding (SAD)**：
   - 定义轻量残差细化算子 \(R(X) = X + \gamma \cdot \sigma(\text{Norm}(\text{PW}(\text{DW}(X))))\)，其中 DW 为深度卷积，PW 为点卷积，\(\gamma\) 初始化为 0。
   - **尺度内更新**：对各层级独立应用 \(R\) 得到 \(L_k\)。
   - **尺度间传播**：从最粗尺度开始，递归融合：\(X_K = R(L_K)\)，\(X_k = R(\text{Up}(X_{k+1}; L_k) + L_k)\)。
   - 最后使用线性预测头（1×1 卷积）从最细尺度 \(X_1\) 输出分割图 \(Y\)。

### 2.2 关键设计特点
- 骨干冻结，仅 TPA 和 SAD 模块可训练（共 6.08M 参数）。
- TPA 不引入复杂上采样，仅用 reshape + 1×1 卷积 + 步长卷积实现伪金字塔。
- SAD 的残差算子极轻量，单次操作仅含 depthwise + pointwise conv + group norm + GELU。

## 3. 实验设计
### 3.1 数据集
| 数据集 | 类型 | 样本量 | 任务 |
|--------|------|--------|------|
| PanCT（自建） | CT 胰腺肿瘤 | 284 例患者（243 训练/41 测试） | 小病灶分割 |
| TN3K | 甲状腺超声 | 3493 张 | 结节分割 |
| Kvasir-SEG | 结肠镜息肉 | 1000 张 | 息肉分割 |
| ISIC | 皮肤镜 | 2750 张 | 皮肤病变分割 |

- PanCT 由中山大学肿瘤防治中心两个放射科医生独立标注，所有图像预处理为 256×256 的 2D 切片。
- 公开数据集按官方划分进行训练/测试。

### 3.2 对比方法
U-Net、SegNet、R2U-Net、Attention U-Net、TransUNet、U-NeXt、U-KAN 等 7 种代表性方法。评估指标为 Dice 相似系数（DSC）和 95% Hausdorff 距离（HD95）。

## 4. 资源与算力
- **GPU**：4 块 NVIDIA RTX A6000（48GB）。
- **优化器**：AdamW，学习率 1e-4，权重衰减 1e-4。
- **训练轮次**：50 epoch，batch size = 4。
- **文中未给出总训练时长或单轮时长**，仅能推断为中等量级（约数小时至十几小时）。

## 5. 实验数量与充分性
- **对比实验**：在 4 个数据集上各与 7 种方法比较，所有方法采用相同输入尺寸和评估协议。
- **消融实验**：表 2 在 TN3K 和 PanCT 两个数据集上对比了 Basic（无 TPA/SAD）、M1（仅 TPA）、M2（仅 SAD）和完整模型，验证各组件贡献。
- **效率分析**：图 3 展示了参数-性能散点图、FPS-性能对比，证明 SegDINO 在轻量和精度间取得平衡。
- **定性可视化**：图 2 提供了 4 个数据集上代表性样例的预测对比。
- **总体评价**：实验设计较为充分，数据集覆盖多个模态和病灶难度，消融明确，对比方法多样，评估指标客观（DSC、HD95）。但消融仅在两个数据集上展开，未在所有数据集中完全分解。

## 6. 论文的主要结论与发现
- SegDINO 在所有四个数据集上取得 **最先进（SOTA）性能**，尤其在小病灶（PanCT）上优势显著（DSC 提升 2%–8%）。
- TPA 是主要贡献模块，SAD 提供额外精度提升；对于大而连续的目标，两者增益较小；对于微小目标，TPA 带来巨大改善。
- 证明了“**在强自监督骨干基础上，仅需轻量多尺度建模即可实现高效高精度分割**”，避免重型解码器。

## 7. 优点
- **轻量高效**：仅添加 6.08M 参数（骨干冻结），推理速度达 51 FPS，远超多数 Transformer 方法。
- **设计简洁有效**：TPA 无需复杂上采样，SAD 残差算子参数量极低，易于训练和部署。
- **自建难点数据集**：PanCT 专注于胰腺小肿瘤，补充了现有基准中缺乏的细粒度小病灶场景。
- **消融与分析清晰**：明确揭示 TPA 与 SAD 的各自贡献及与目标大小的关联。
- **公平性**：对比方法均采用相同输入尺寸和训练策略，避免作弊。

## 8. 不足与局限
- **仅限 2D 分割**：未利用 CT 体积的 3D 上下文，作者承认该限制并留作未来工作。
- **骨干单一**：仅测试 DINOv3-S，未评估更大骨干（如 ViT-B/L）或不同自监督版本（DINOv2）。
- **PanCT 未公开**：仅声称可合理请求获得，可复现性受限。
- **对比方法未包含最新 SAM 系列**（如 SAM2-Med、MedSAM），仅提及 SAM 系列计算成本高，但未直接比较性能。
- **消融范围受限**：仅在两个数据集上展示模块消融，未在全部四个数据集上进行完整分解。
- **训练轮次较少**：50 epoch 可能未充分收敛，尤其对于大模型或困难数据。
- **跨模态泛化未知**：仅涉及 CT、超声、内镜、皮肤镜，未测试 MRI、X 射线等模态。
- **PanCT 可能引入机构偏差**：数据来自单一医院，患者群体有限。

（完）
