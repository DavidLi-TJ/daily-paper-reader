---
title: Learning a Semantic Calibration Network for Open-Vocabulary Semantic Segmentation
title_zh: 学习语义校准网络用于开放词汇语义分割
authors: "Yang Sun, Tao Wang, Anastasia Ioannou, Ge Xu"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08001v1"
tags: ["query:raw-seg"]
score: 9.0
evidence: 深度学习开放词汇语义分割
tldr: 针对开放词汇语义分割任务，现有方法依赖特征聚合或简单微调，未能充分建模类别间语义相关性，导致判别力不足。提出语义校准网络（SCN），通过类消歧和逻辑融合模块显式建模类别语义相关性，增强模型判别力同时保留CLIP泛化能力。在多个主流基准上相比现有最优方法取得显著性能提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1568, \"height\": 743, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1408, \"height\": 731, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08001v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1415, \"height\": 994, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 682, \"height\": 685, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1869, \"height\": 469, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08001v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 194, \"label\": \"Table\"}]"
motivation: 现有方法依赖特征聚合或简单微调，未能充分建模类别间语义相关性，导致判别力不足。
method: SCN包括类消歧模块（交叉注意力产生视觉感知伪文本嵌入）和逻辑融合模块（动态整合多模态语义证据）。
result: 在多个主流基准上相比现有最优方法取得显著性能提升。
conclusion: 通过显式建模类别语义相关性和动态融合逻辑，有效提升开放词汇语义分割的判别力与泛化性。
---

## 摘要
语义图像分割为每个像素分配预定义的类别标签，近年来取得了显著进展。开放词汇分割（OVS）将分割任务从固定集合扩展到开放集合，使得能够基于任意文本输入（例如类别名称或描述）识别和分割新概念。在本文中，我们提出了一种新颖的语义校准网络（SCN）用于开放词汇语义分割。与以往专注于特征聚合或简单微调预训练模型的方法不同，SCN通过显式建模类别之间的语义相关性来细化掩码分类过程，旨在增强模型的判别能力，同时有效保持预训练CLIP模型的泛化能力。具体来说，SCN包含两个核心组件：类别消歧（CD）和逻辑融合（LF）。首先，利用交叉注意力机制将文本嵌入转换为具有视觉感知能力的伪文本嵌入，以得出增强的相似度分数，补充原始的掩码-文本相似度分数。随后，类别消歧模块通过残差架构捕获隐式类别间依赖关系，有效解决语义歧义。最后，逻辑融合模块动态整合多方面的语义证据，确保模型在保持CLIP固有泛化能力的同时达成稳健的语义共识。在主流基准上的全面实验结果表明，与最先进算法相比，所提方法实现了显著的性能提升。

## Abstract
Semantic image segmentation assigns a predefined category label to each pixel, has achieved significant progress lately. Open-Vocabulary Segmentation (OVS) extends the segmentation task from a fixed set to an open set, enabling the identification and segmentation of novel concepts based on arbitrary text inputs, such as category names or descriptions. In this paper, we propose a novel Semantic Calibration Network (SCN) for open-vocabulary semantic segmentation. Different from prior approaches that focus on feature aggregation or simple fine-tuning of pre-trained models, SCN refines the mask classification process by explicitly modeling the semantic correlations between classes, aiming to enhance the model's discriminative power while effectively preserving the generalization abilities of the pre-trained CLIP model. Specifically, SCN comprises two core components: Class Disambiguation (CD) and Logits Fusion (LF). First, a cross-attention mechanism is utilized to transform the text embeddings into visually aware pseudo-text embeddings, in order to derive an enhanced similarity score that complements the original mask-text similarity score. Subsequently, the Class Disambiguation module captures implicit inter-class dependencies through a residual architecture to effectively resolve semantic ambiguities. Finally, the Logits Fusion module dynamically integrates multifaceted semantic evidence to ensure that the model achieves a robust semantic consensus while maintaining CLIP's inherent generalization capability. Comprehensive experimental results on mainstream benchmarks demonstrate that the proposed method achieves significant performance improvements compared to state-of-the-art algorithms.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **任务**：开放词汇语义分割（OVS），目标是用任意文本描述（如类别名称或描述）分割图像中的物体，无需在训练中见过这些类别。
- **现有方法的局限**：主流方法采用“先分割后识别”范式（如使用CLIP），但CLIP是全局图像-文本对齐，存在空间粒度与像素级分割的天然差距。简单微调CLIP容易导致对训练集类别的过拟合，损害零样本泛化能力。此外，语义混淆（semantic confusion）在分类阶段频繁出现，成为性能瓶颈。
- **核心问题**：如何在不破坏CLIP零样本泛化能力的前提下，显式建模类别间语义相关性，增强模型对语义模糊类别的判别力。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
- 提出**语义校准网络（SCN）**，直接在相似度（logit）空间中建模类别间依赖关系，通过两个模块（Class Disambiguation 和 Logits Fusion）对原始掩码-文本相似度进行校准，同时保留CLIP的原始相似度相对顺序，以维持零样本泛化能力。

### 2.2 整体框架
- 输入：图像 \(I\) 和文本描述集合 \(\{t_i\}\)。
- 视觉编码器提取视觉特征 \(F_v\)（维度 \(C \times H \times W\)），文本编码器提取文本特征 \(F_t\)（维度 \(C \times N_C\)）。
- 掩码生成器 \(\mathcal{M}\) 生成 \(N\) 个二值掩码提议 \(\{\hat{m}_i\}\)。
- 掩码池化（Mask Pooling）聚合得到掩码特征 \(F_m \in \mathbb{R}^{C \times N}\)。
- 原始相似度分数 \(S = F_m^T F_t \in \mathbb{R}^{N \times N_C}\)。
- SCN 接收 \(F_v, F_t, F_m\)，输出校准后的相似度分数 \(Q\) 用于分类。

### 2.3 关键技术细节

#### (a) 增强相似度分数 \(T\)
- 通过交叉注意力机制，以文本特征为Query，视觉特征为Key/Value，将文本嵌入转换为“视觉感知的伪文本嵌入” \(F_t'\)，再计算 \(T = F_m^T F_t'\)。
- 引入残差连接：\(F_t' = \text{Norm}(F_t + P_t)\)，其中 \(P_t = \text{Attention}(q(F_t), k(F_v), v(F_v))\)。

#### (b) 类别消歧模块（CD）
- 目标：消除原始相似度 \(S\) 中的语义歧义。
- 结构：三个适配器（Adapter）\(A_1, A_2, A_3\)，采用残差架构。
  - \(A_1\) 从 \(S\) 中学习隐式类别间依赖。
  - \(A_2\) 从掩码特征 \(F_m\) 中提取语义歧义先验。
  - \(A_3\) 联合处理 \(A_1\) 和 \(A_2\) 的输出，产生残差项。
- 输出：\(R = S + A_3(A_1(S) + A_2(F_m))\)。

#### (c) 逻辑融合模块（LF）
- 输入：校准后的相似度 \(R\) 和增强相似度 \(T\)。
- 权重生成器（Generator）：从掩码特征 \(F_m\) 动态生成融合权重 \(\alpha = \text{Generator}(F_m)\)。
- 最终分数：\(Q = \alpha T + (1-\alpha) R\)。

### 2.4 训练
- 所有SCN参数（增强相似度中的投影层、适配器、生成器）与其他网络联合端到端训练，使用标准交叉熵损失。

## 3. 实验设计

### 3.1 训练数据集
- **COCO-Stuff**（约118k图像，171类）用于训练。

### 3.2 测试基准
- **ADE20K**：A-150（150类）、A-847（847类）。
- **PASCAL-VOC (PAS-20)**：20类。
- **PASCAL-Context**：PC-59（59类）、PC-459（459类）。

### 3.3 评估指标
- **mIoU**（平均交并比）。

### 3.4 对比方法
- 包括 ZegFormer, DeOP, OvSeg, SAN, OpenSeg, EBSeg, SED, CAT-Seg, MAFTP, MaskAdapter 等。表中按VLM规模分类（Base-scale VLM）。

## 4. 资源与算力

- **GPU**：2块 NVIDIA RTX A6000。
- **训练轮次**：5 epochs。
- **批次大小**：12。
- **优化器**：AdamW，初始学习率 \(1\times10^{-4}\)，权重衰减0.05。
- **图像尺寸**：训练裁剪1024×1024，评估896×896。
- 注意：文中未明确报告总训练时间或吞吐量。

## 5. 实验数量与充分性

- **主实验结果**（表II）：在5个基准（A-150, A-847, PC-59, PC-459, PAS-20）上对比了10+方法，显示SCN在A-150、A-847、PC-59上达到最佳，在PC-459和PAS-20上虽非最优但接近。
- **消融实验**（表III）：依次加入CD、LF模块，验证各组件贡献（A-150从35.6→35.9→36.2）。
- **定性结果**（图3）：展示2个样本图（实际图中有3个样本），对比基线MaskAdapter，显示SCN纠正了语义混淆（如“chair”与“armchair”）。
- **充分性评价**：
  - 实验覆盖多个主流开放词汇分割基准，包含不同粒度（150类～847类）。
  - 消融实验清晰验证了每个模块的有效性。
  - 但缺少与更多最新方法（如MaskCLIP++、ESC-Net等）的对比；未提供推理速度或参数量对比。

## 6. 论文的主要结论与发现

- SCN通过显式建模类别间语义相关性（CD模块）和动态融合多源相似度（LF模块），显著提升了开放词汇语义分割的准确性。
- 在A-150、A-847、PC-59上取得了最高mIoU（36.2、14.7、58.6），相比基线方法（MaskAdapter）均有提升。
- 关键发现：在相似度空间中操作（而非特征空间）可以更好地保留CLIP的零样本泛化能力，同时增强判别力。

## 7. 优点

- **创新性**：首次在开放词汇分割中显式地在相似度（logit）空间建模类别间依赖，而非仅做特征聚合或微调。
- **有效性**：两个模块设计简洁（残差适配器+动态权重生成），在多个基准上取得一致提升，且消融实验验证了各组件贡献。
- **通用性**：方法可集成到现有的“分割-识别”框架中，不依赖特殊骨干网络。
- **泛化保持**：通过保持原始相似度相对顺序和残差结构，避免过拟合，维持零样本能力。

## 8. 不足与局限

- **实验覆盖不全面**：未与近期最强方法（如MaskCLIP++、ESC-Net）直接比较；未提供在COCO-Stuff本身上的性能（仅报告了训练数据集，但test结果只在其他基准上）。
- **计算资源开销**：SCN引入了额外适配器和生成器，但未给出参数量、FLOPs、推理速度等指标，难以评估实际效率。
- **消融实验深度不足**：仅做了组件有无的对比，未对不同融合策略（如固定权重 vs 动态权重）或不同注意力机制变体进行深入分析。
- **定性结果样本偏少**：仅展示了2个样本图（实际图3含3个样本），且未展示失败案例。
- **应用限制**：所有实验基于COCO-Stuff训练，对其他领域（如医学、遥感）的通用性未验证；依赖于CLIP的视觉编码器（ConvNeXt-B），替换其他骨干可能影响性能。
- **潜在偏差**：训练类与测试类完全不相交（Ctest ∩ Ctrain = ∅），但实际场景中可能存在重叠，文中未讨论此情况。

（完）
